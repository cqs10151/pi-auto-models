{
  "name": "pi-auto-models",
  "version": "0.1.9",
  "description": "Auto-switch pi between a primary and fallback model based on quota, with /auto-model command.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Fatpandac/pi-auto-models.git"
  },
  "license": "MIT",
  "scripts": {
    "release": "npm version patch && git push --follow-tags origin main"
  },
  "keywords": [
    "pi-package"
  ],
  "files": [
    "index.ts",
    "quota-data.ts",
    "quota-utils.ts",
    "README.md",
    "LICENSE"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  }
}
