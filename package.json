{
  "name": "pi-auto-models",
  "version": "0.1.2",
  "description": "Auto-switch pi between a primary and fallback model based on quota, with /usage and /auto-model commands.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Fatpandac/pi-auto-models.git"
  },
  "license": "MIT",
  "scripts": {
    "publish": "npm version patch && git push --follow-tags origin main"
  },
  "keywords": [
    "pi-package"
  ],
  "files": [
    "index.ts",
    "quota-data.ts",
    "format.ts",
    "quota-utils.ts",
    "README.md",
    "LICENSE"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  }
}
